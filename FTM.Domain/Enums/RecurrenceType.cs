namespace FTM.Domain.Enums
{
    public enum RecurrenceType
    {
        None = 0,    // Không lặp (mặc định)
        Daily = 1,   // Lặp hàng ngày
        Monthly = 2, // Lặp hàng tháng
        Yearly = 3   // Lặp hàng năm
    }
}